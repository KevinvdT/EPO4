import React, { Component } from 'react';
import styled from 'styled-components';

const Container = styled.div`
  flex: 0 0 auto;
  width: 16rem;
`;

export default class ConfigPanel extends Component {
  render() {
    return <Container>ConfigPanel</Container>;
  }
}
